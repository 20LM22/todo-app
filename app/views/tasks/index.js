const checkedBtns = document.getElementsByClassName("checked");

checkedBtns.forEach(function(checkedBtn) {
    checkedBtn.addEventListener("click", function() {
        /* need to save the button as being clicked to the database */
        
    });
});